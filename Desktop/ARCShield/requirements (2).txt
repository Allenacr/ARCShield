# Bidirectional Transaction Guard — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages
# Covers dev through deployment: API, feature engineering, ML model +
# explainability, graph service, rule engine, database access, synthetic
# data generation, inter-bank intelligence / federated simulation, push
# notifications, production serving, error tracking, and testing.

# ── API service ─────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.2
orjson==3.10.7               # fast JSON — matters for the sub-200ms p95 target
email-validator==2.2.0       # required by pydantic for Supabase Auth email fields

# ── Production serving (Render) ──────────────────────────────
gunicorn==23.0.0              # process manager in front of uvicorn workers

# ── Auth ──────────────────────────────────────────────────────
PyJWT[crypto]==2.9.0          # verify Supabase-issued JWTs on protected endpoints

# ── Database (Supabase / Postgres) ──────────────────────────
supabase==2.7.4
psycopg[binary]==3.2.1
SQLAlchemy==2.0.35
alembic==1.13.2

# ── Caching / rate limiting ───────────────────────────────────
redis==5.0.8

# ── Data handling ────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==17.0.0

# ── Machine learning + explainability ───────────────────────
scikit-learn==1.5.1
xgboost==2.1.1
lightgbm==4.5.0
shap==0.46.0
imbalanced-learn==0.12.3
joblib==1.4.2

# ── Graph service ────────────────────────────────────────────
networkx==3.3

# ── Synthetic data generation ───────────────────────────────
Faker==28.4.1
mimesis==18.0.0

# ── Security / hashing (inter-bank intelligence, F-28) ──────
cryptography==43.0.1

# ── Push notifications ───────────────────────────────────────
firebase-admin==6.5.0

# ── Federated learning simulation (F-29) ─────────────────────
# XGBoost/LightGBM above are reused for local training; no extra
# federated framework is required for a simulated coordinator.

# ── Background jobs / scheduling (model refresh, hold expiry) ─
apscheduler==3.10.4

# ── Monitoring / logging / error tracking ────────────────────
structlog==24.4.0
prometheus-client==0.20.0
sentry-sdk[fastapi]==2.14.0

# ── Testing ────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
factory-boy==3.3.1

# ── Dev tooling (not needed in production, keep in a separate
#    requirements-dev.txt if you want a lean prod image) ─────
ruff==0.6.9
mypy==1.11.2
black==24.8.0
